from src.model.base import Base


class MGWR(Base):
    pass

    # def __init__(self,
    #              dataset: SpatialDataset,
    #              kernel: IKernel,
    #              logger: ILogger) -> None:
    #     super().__init__(dataset, kernel, logger)
